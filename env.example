# AWS S3 Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_S3_BUCKET=your-bucket-name
# Optional: Custom S3 endpoint (for MinIO, Alibaba Cloud OSS, Tencent Cloud COS, etc.)
# Leave empty to use default AWS S3 endpoint
# Note: The application uses path-style access by default (e.g., https://s3.region.amazonaws.com/bucket/key)
# Examples:
# AWS_ENDPOINT=http://localhost:9000 (MinIO)
# AWS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com (Alibaba Cloud OSS)
AWS_ENDPOINT=

# Server Configuration
PORT=3001
